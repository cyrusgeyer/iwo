mode: independent
per_factor_training: [True, True, True, True, True]
default:
  first_dim: 10
  num_hidden_layers: 4
  hidden_dim: 25
  batch_norm: False
  init_w: kaiming_uniform_custom #,xavier_normal, xavier_uniform, kaiming_normal, eye
  init_layers: kaiming_uniform_custom #,xavier_normal, xavier_uniform, kaiming_normal, eye
  nonlinearity: leaky_relu # relu, leaky_relu
  nonlinearity_param: 0.01
  first_layer_batch_norm: False
  first_layer_nonlinearity: False 
factor_0:
factor_1:
factor_2:
factor_3:
factor_4: